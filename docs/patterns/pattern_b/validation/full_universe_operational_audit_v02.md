# Pattern B 전체 종목 운영 감사 V02

> 상태: 감사 완료. 판정은 `PASS`다. [운영 계약 V02](../spec/production_contract_v02.md)의 두
> 정책(시장 이전 이력 연속성, 가격 신선도)을 넣고, [감사 V01](full_universe_operational_audit_v01.md)과
> 같은 기준일·모집단에 한 번 다시 적용했다. 규칙, 지표 산식, 임계값, evaluator의 상태 판정은
> 바꾸지 않았다. 일일 갱신·웹에는 연결하지 않았다. 감사 V01 기록과 산출물은 그대로 둔다.

실행 스크립트: `scripts/audit_pattern_b_full_universe_v02.py`. 모집단·종목 구간·불변식 함수는 V01
스크립트의 것을 바꾸지 않고 불러 썼다.

산출물 (`artifacts/patterns/pattern_b/operational_audit_v02/2026-09-21/`):

- `pattern_b_full_universe_results.csv`: 종목별 결과 2,544행
- `pattern_b_full_universe_summary.json`: 집계와 점검 결과

## 1. 기준일과 모집단

V01과 같다.

- 기준일: 2026-09-21. rolling authority 인증 경계와 같은 날이다.
- Basic Info 스냅샷: 2026-09-21
- 모집단: `load_target_pit_common_tickers`, 2,544종목 (KOSPI 808, KOSDAQ 1,736)
- 종목 구간: 모두 정확히 1개. 없음·겹침·시장 불일치는 0건
- 처리: 2,544종목 전부
- 저장소 `DATA_UNAVAILABLE`: 0건
- 오류: 0건
- 실행 시간: 약 511초

## 2. 시장 이전 이력 (정책 A)

기준일 구간 앞에 같은 종목코드의 구간이 있는 종목은 31개다.

| 판정 | 종목 수 | 내용 |
|---|---|---|
| 연결 | 19 | 모두 KOSDAQ → KOSPI, 같은 `isu_cd`, 다음 거래일에 이어짐. 모두 구간 2개를 실제로 로드함 |
| 거절 `GAP` | 12 | 거래일 공백. 아래 참고 |
| 거절 `ISU_CHANGE`·`NON_COMMON`·`SAME_MARKET` | 0 | |
| 판단 불가 (겹침·모호) | 0 | |

거절된 12종목:

- 2종목은 KOSDAQ 구간이 2015~2016년에 끝나고 2024~2025년에 다시 시작한다(수년 뒤 재상장).
  V01과 같이 `UNAVAILABLE`이다.
- 10종목은 KOSDAQ 구간이 2011-04-29에 끝나고 73~665거래일 뒤 KOSDAQ에서 다시 시작한다. 시장
  이전이 아니다. 현재 구간만으로 `READY`다.

연결 전(활성 구간만)과 연결 후 비교 (연결 19종목):

| 변화 | 종목 수 |
|---|---|
| `UNAVAILABLE` → `READY` | 3 (상태: `DEPRESSED` 2, `OVERHEATED` 1) |
| `READY` → `READY`, 상태 같음 | 16 |
| 상태가 바뀐 `READY` 종목 | 0 |

감사 V01에서 확인한 시장 이전 3종목은 모두 연결되어 `READY`가 되었고, 재상장 2종목은 연결되지
않았다. 나머지 16종목은 활성 구간이 이미 36개월 이상이다. 유지 지표가 최근 봉만 쓰므로 값이
같다.

## 3. 가격 신선도 (정책 B)

기대 주봉(`expected_weekly_bar`)은 2026-09-18이다.

| 구분 | `CURRENT` | `STALE` | 합계 |
|---|---|---|---|
| `READY` | 2,188 | 89 | 2,277 |
| `UNAVAILABLE` | 259 | 8 | 267 |
| 합계 | 2,447 | 97 | 2,544 |

- `STALE` 97개 중 96개는 마지막 주봉이 2026-09-18보다 이르다. V01의 "마지막 주봉이 늦은
  종목" 96개와 같은 집합이다.
- 나머지 1개는 완료 주봉이 아예 없다. 기준일 주에 상장한 종목이다. V01의 집계는 주봉이 있는
  경우만 셌으므로 이 종목이 빠져 있었다.
- `READY` + `STALE` 89종목은 Pattern B 상태를 그대로 두고, 신선도만 `STALE`로 표시했다.

## 4. 평가 결과

| 구분 | 전체 | KOSPI | KOSDAQ |
|---|---|---|---|
| `READY` | 2,277 | 781 | 1,496 |
| `UNAVAILABLE` | 267 | 27 | 240 |

| 상태 (`READY` 기준) | 전체 | KOSPI | KOSDAQ |
|---|---|---|---|
| `DEEP_DEPRESSED` | 363 | 68 | 295 |
| `DEPRESSED` | 971 | 273 | 698 |
| `NORMAL` | 753 | 344 | 409 |
| `OVERHEATED` | 157 | 80 | 77 |
| `EXTREME_OVERHEATED` | 33 | 16 | 17 |

V01 대비 차이는 시장 이전 연결 3종목뿐이다(`READY` +3, `DEPRESSED` +2, `OVERHEATED` +1). 분포는
진단 자료이며 기준을 두지 않는다.

`UNAVAILABLE` 267건의 사유는 모두 `INSUFFICIENT_BARS`다.

| 계산 불가 유지 지표 | 건수 |
|---|---|
| 36개월 범위 위치만 | 97 |
| 36개월 범위 위치 + 24개월 이동평균 이격 | 104 |
| 세 지표 모두 | 66 |

## 5. 구조 점검

모두 0건 또는 일치다.

- V01 점검 항목:
  - 중복·미처리·추가 종목
  - 종목 구간 모호
  - 저장소 `DATA_UNAVAILABLE`, 오류
  - 구간 경계 위반, 기준일 이후 행, 출처 불일치
  - 알 수 없는 평가 상태·Pattern B 상태
  - `READY`인데 상태 없음, `UNAVAILABLE`인데 상태 있음
  - 버전 불일치
- 정책 A 점검 항목:
  - 시장 이전 사슬 모호
  - 연결 구간 경계 위반, 날짜 중복, 날짜 순서 위반
  - 정책에 맞지 않는 연결 (연결마다 따로 다시 확인)
- 정책 B 점검 항목:
  - 알 수 없는 신선도
  - `CURRENT`인데 마지막 주봉이 기대값과 다름
  - `STALE`인데 마지막 주봉이 기대값과 같음
- 일치 확인:
  - 처리 행 수 = 모집단 수 (2,544)
  - `READY` + `UNAVAILABLE` = 모집단 수 (2,277 + 267)
  - 5개 상태 합 = `READY` 수

## 6. 재현성과 V01 대비 확인

- 재실행: V01과 같은 방식(종목코드 SHA-256 순서 앞 20개)으로 골라, 새 저장소 인스턴스로 다시
  평가했다. 불일치는 0이다.
  - V01 비교 필드에 더해 다음도 비교했다: 이력 구간 사슬, `history_effective_from`,
    `market_transfer_stitched`, 신선도.
- V01 결과와 비교: 2,544종목 중 결과가 다른 종목은 시장 이전 연결 3종목뿐이다. 연결하지 않은
  2,541종목은 평가 상태·상태·원시값·마지막 봉이 모두 V01과 같다.
- authority 지문:
  - V02 실행 전후가 같았다.
  - V01 실행 뒤와 비교하면 manifest·병합 PIT·병합 달력 파일은 같다.
  - 데이터 폴더(main 작업 폴더)의 Git 상태만 달랐다. 그 사이 main 작업 폴더에서 코드 파일
    수정이 있었다.
  - 이 감사는 pattern-b 브랜치 코드로 실행했고, main 작업 폴더는 데이터만 읽었다.

## 7. 판정: `PASS`

- 모집단 2,544종목을 모두 처리했다.
- 종목 구간, 저장소, 계약, 정책 A, 정책 B의 구조 위반은 0건이다.
- 저장소 `DATA_UNAVAILABLE`은 0건이다.
- 재실행 20건이 모두 일치했다.
- 규칙·지표·evaluator·공용 코드는 바꾸지 않았다.
